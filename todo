- make directives not a breakpoint
- see the difference between continue from step and continue from breakpoint (make the former work and respect subsequent breakpoints)
- GUI buttons to step into, stop, etc
- edit text in the editor
- recently changed register change font colour (red, orange, yellow)
- figure out memory layout and file IO
- figure out a program counter of sorts
- add a console, impl syscalls
- reload button to reload new file code changes
    - automatically detect changes and hot-reload?
- add a watch panel, where users can specify the debugger to stop when a register changes or when a certain 
    condition is met like $t0 < 9 or $t0 == $a1
- add help for limitations and license